
# Changes by Copilot (Checkpoint 1.2)

- Added `css/styles.css` with a cool blue theme and Google Font **Outfit**.
- Inserted stylesheet link into `<head>` of both HTML files.
- Converted mistaken `<header>` in `about.html` to `<head>` so CSS loads.
- Added `class="container"` to `<main>` for nicer line lengths.
- Tidied an extra stray `</section>` / image placement in `about.html`.
- Created `.bak` backups of the original HTML files next to each file.

